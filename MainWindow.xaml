<Window x:Class="Avtorizatzia.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Avtorizatzia"
        mc:Ignorable="d"
        Title="Вход в систему" 
        Height="250" Width="350"
        Background="#E8F0FE" ResizeMode="NoResize" Closing="Close_Click">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Вход в систему" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,10" Foreground="#1565C0"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Логин:" Margin="0,0,0,5"/>
            <TextBox x:Name="txtLogin" Padding="5" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,5,0,0">
            <TextBlock Text="Пароль:" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" Padding="5" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btnLogin" Content="Войти" 
                    Click="BtnLogin_Click"/>

            <Button x:Name="btnExit" Content="Выход" 
                    Click="BtnExit_Click"/>
        </StackPanel>
    </Grid>
</Window>
